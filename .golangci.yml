linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - gosimple
    - ineffassign
    - unused
    - gofmt
    - misspell

linters-settings:
  errcheck:
    check-type-assertions: true

run:
  timeout: 5m
  modules-download-mode: readonly
